# Frontend-specific ignores (root .gitignore is owned by the delivery lead).
node_modules
dist
dist-ssr
*.local

# Env files — only .env.example is committed.
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.DS_Store

# Test / coverage
coverage
